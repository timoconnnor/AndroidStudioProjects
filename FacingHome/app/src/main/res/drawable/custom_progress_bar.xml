<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background -->
    <item android:id="@android:id/background">
        <shape>
            <solid android:color="#3498db" /> <!-- Background color -->
            <corners android:radius="5dp" /> <!-- Adjust the radius if needed -->
        </shape>
    </item>

    <!-- Outline -->
    <item>
        <shape>
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="2dp"
            android:color="#000000" /> <!-- Outline color -->
            <corners android:radius="5dp" /> <!-- Adjust the radius if needed -->
        </shape>
    </item>

    <!-- Progress -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <solid android:color="#CCCCCC" /> <!-- Progress color -->
                <corners android:radius="3dp" /> <!-- Adjust the radius if needed -->
            </shape>
        </clip>
    </item>

</layer-list>